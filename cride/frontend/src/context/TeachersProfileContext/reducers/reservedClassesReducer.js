
export const reservedClassesReducer = (state, action) => {
    switch (action.type) {
        case 'REQUEST_RESERVED_CLASS':
            return state
        default:
            return state;
    }
}